---
server:
  port: 7777
  servlet:
    context-path: /uaa

spring:
  application:
    name: demo-auth-server

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8282/eureka}